# tester-agent
A langgraph based agent that can read the java files in the selected folder and then create test cases for the java files inside that folder.

run the program
not possible now

run the backend
cd backend/src/app
uvicorn main:app --reload

Use tools like postman or swagger to check endpoints
